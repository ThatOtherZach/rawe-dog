{
  "name": "@workspace/tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:digest": "playwright test digest-cohesion"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
